export const Button = ({children}) => {
  return (
    <button className='text-white font-bold-600 w-64 py-4 rounded text-xl bg-gradient-to-r from-green-400 to-blue-500 hover:from-pink-500 hover:to-yellow-500 ...'>{children}</button>
  )
}
